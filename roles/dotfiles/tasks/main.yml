- name: checkout my dotfiles
  git:
    repo: https://github.com/bryutus/dotfiles.git
    dest: ~/.dotfiles

- name: create symbolic link
  file:
    src: ~/.dotfiles/{{ item }}
    dest: ~/{{ item }}
    state: link
    force: yes
  with_items: "{{ dotfiles }}"

